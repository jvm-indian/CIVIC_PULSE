rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /issues/{issueId} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false; // All direct client access is blocked. Everything is securely proxied via server.ts!
    }
  }
}
